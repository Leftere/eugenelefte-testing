<div class="icon-cards">
  {% for item in items %}
  <div class="icon-card">
    <img src="{{ item.image.src }}" alt="{% if item.image.alt %}{{ item.image.alt }}{% endif %}" loading="lazy" class="icon-card__icon">
    <p class="icon-card__text">{{ item.text|raw }}</p>
  </div>
  {% endfor %}
</div>